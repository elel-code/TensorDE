[package]
name = "tensor-drm"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "Value-only DRM topology and output policy helpers (no libdrm/Smithay)"
license = "MIT OR Apache-2.0"

[dependencies]
tensor-host = { path = "../tensor-host" }
tensor-util = { path = "../tensor-util" }
thiserror = "2"
