//! **wlroots / wlr** community protocols (`zwlr_*`, via `wayland-protocols-wlr`).
//!
//! - `layer_shell` — bound and used by [`crate::native::NativeShell::create_layer_surface`]
//!   and bufferless [`crate::native::NativeShell::create_layer_surface_gpu`] (Vulkan WSI)
