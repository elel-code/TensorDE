[package]
name = "tensor-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "Smithay-free protocol identities, scene values, and surface lifecycle policy"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
tensor-util = { path = "../tensor-util" }

[dev-dependencies]
serde_json = "1"
