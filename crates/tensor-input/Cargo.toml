[package]
name = "tensor-input"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "Value-only input samples and device capability tables (no libinput/Smithay)"
license = "MIT OR Apache-2.0"

[dependencies]
tensor-event = { path = "../tensor-event" }
tensor-host = { path = "../tensor-host" }
