[package]
name = "tensor-present"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "Value-only present intents and scanout readiness (no KMS FDs)"
license = "MIT OR Apache-2.0"

[dependencies]
tensor-host = { path = "../tensor-host" }
thiserror = "2"
