[package]
name = "tensor-launcher"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"
publish = false
description = "Retained, bounded application launcher for TensorDE"

[dependencies]
tensor-kdl = { path = "../../crates/tensor-kdl" }
thiserror = "2"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
