[package]
name = "tensor-msg"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MPL-2.0"
publish = false
description = "Standalone control client for Tensor products"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
tensor-ipc = { path = "../../crates/tensor-ipc" }
thiserror = "2"

[lints]
workspace = true
